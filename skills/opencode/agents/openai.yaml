interface:
  display_name: "OpenCode for Codex"
  short_description: "Request tasks such as implementation or review from OpenCode. Use this only when instructed by the user."
  default_prompt: "Use $opencode to carry out the requested task in the selected repository."
