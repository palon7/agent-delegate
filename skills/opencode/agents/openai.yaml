interface:
  display_name: "OpenCode"
  short_description: "Request tasks such as implementation or review from OpenCode. Use this only when instructed by the user."
  default_prompt: "Use $delegate:opencode to carry out the requested task in the selected repository."
